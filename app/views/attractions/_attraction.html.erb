<p>
  <% if is_admin? %>
    <%= link_to "Show #{attraction.name}", attraction_path(attraction) %>
  <% else %>
    <%= attraction.name %> - <%=link_to "Go on #{attraction.name}", attraction_path(attraction)%>
  <% end %>
</p>